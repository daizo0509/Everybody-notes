<div class=" col-xs-offset-2 .col-xs-8s .col-xs-offset-2">
	<h1>タグ管理ページ</h1>

		<% @tags.each do |tag| %>
	    	<div class="p-item">
			    <div class="p-title">
			    	<h3><%=tag.name %></h3>
			    </div>
			    <p>destroy</p>
	        	<!-- <td><%#= link_to 'Destroy', tag, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        	</div>
		<% end %>
		<%#= paginate @tags, class: "pagenate" %>
		<table>
  <thead>
    <tr>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
    
      <tr>
        <td><h4><%= tag.name %></h4></td>
        <td></td>
        <td>destroy</td>
        
        <!-- <td><%#= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

</div>